<!DOCTYPE html>
<html lang="uk">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Геометрія: Всі Фігури</title>
  <style>
    * { box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', Tahoma, sans-serif;
      background: linear-gradient(135deg, #e8f5e8, #e3f2fd);
      color: #0d47a1;
      margin: 0;
      padding: 12px;
      line-height: 1.5;
    }
    .container {
      max-width: 980px;
      margin: 0 auto;
      background: white;
      padding: 20px;
      border-radius: 16px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.1);
    }
    h1 {
      text-align: center;
      color: #0d47a1;
      margin-bottom: 10px;
    }
    .tabs {
      display: flex;
      justify-content: center;
      gap: 8px;
      margin: 18px 0;
      flex-wrap: wrap;
    }
    .tab-btn {
      padding: 10px 18px;
      font-size: 16px;
      background: #e3f2fd;
      border: 2px solid #90caf9;
      border-radius: 10px;
      cursor: pointer;
      font-weight: bold;
      color: #0d47a1;
      transition: all 0.25s;
    }
    .tab-btn.active {
      background: #2196f3;
      color: white;
      border-color: #2196f3;
    }
    .tab-content { display: none; }
    .tab-content.active { display: block; }
    .figure {
      background: #f9fbff;
      padding: 18px;
      margin: 16px 0;
      border-radius: 12px;
      border-left: 4px solid #2196f3;
    }
    .figure h3 {
      color: #1565c0;
      margin-top: 0;
      font-size: 19px;
    }
    svg {
      display: block;
      margin: 12px auto;
      max-width: 280px;
      height: auto;
      border: 1px solid #e0e0e0;
      border-radius: 8px;
      background: #ffffff;
    }
    input, button {
      padding: 7px 12px;
      margin: 4px 2px;
      font-size: 14px;
      border: 1px solid #90caf9;
      border-radius: 6px;
    }
    button {
      background: #2196f3;
      color: white;
      cursor: pointer;
      font-weight: bold;
    }
    .result {
      margin-top: 10px;
      padding: 10px;
      background: #e8f5e9;
      border-radius: 6px;
      font-weight: bold;
      color: #2e7d32;
      font-size: 15px;
    }
    .formulas {
      background: #fff8e1;
      padding: 12px;
      border-radius: 8px;
      margin-top: 10px;
      font-size: 15px;
    }
    footer {
      text-align: center;
      margin-top: 25px;
      color: #546e7a;
      font-style: italic;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>📐 Геометрія: Всі Фігури</h1>
    <p style="text-align:center;">Повний довідник з формулами, малюнками і калькуляторами!</p>

    <div class="tabs">
      <button class="tab-btn active" onclick="openTab('flat')">Плоскі фігури</button>
      <button class="tab-btn" onclick="openTab('solid')">Об’ємні фігури</button>
    </div>

    <!-- Плоскі фігури -->
    <div id="flat" class="tab-content active">
      <!-- Квадрат -->
      <div class="figure">
        <h3>Квадрат</h3>
        <svg width="180" height="180" viewBox="0 0 180 180">
          <rect x="40" y="40" width="100" height="100" fill="none" stroke="#1565c0" stroke-width="2"/>
          <text x="90" y="35" text-anchor="middle" font-size="12">a</text>
        </svg>
        <div class="formulas">
          <strong>Площа:</strong> S = a²<br>
          <strong>Периметр:</strong> P = 4a<br>
          <strong>Діагональ:</strong> d = a√2
        </div>
        <input type="number" id="sq-a" placeholder="Сторона a (см)" step="0.1">
        <button onclick="calc('square')">Обчислити</button>
        <div id="res-square" class="result"></div>
      </div>

      <!-- Прямокутник -->
      <div class="figure">
        <h3>Прямокутник</h3>
        <svg width="220" height="140" viewBox="0 0 220 140">
          <rect x="30" y="30" width="160" height="80" fill="none" stroke="#1565c0" stroke-width="2"/>
          <text x="110" y="25" text-anchor="middle" font-size="12">a</text>
          <text x="20" y="70" text-anchor="start" font-size="12">b</text>
        </svg>
        <div class="formulas">
          <strong>Площа:</strong> S = a·b<br>
          <strong>Периметр:</strong> P = 2(a + b)<br>
          <strong>Діагональ:</strong> d = √(a² + b²)
        </div>
        <input type="number" id="rect-a" placeholder="a (см)" step="0.1">
        <input type="number" id="rect-b" placeholder="b (см)" step="0.1">
        <button onclick="calc('rect')">Обчислити</button>
        <div id="res-rect" class="result"></div>
      </div>

      <!-- Трикутник -->
      <div class="figure">
        <h3>Трикутник</h3>
        <svg width="200" height="160" viewBox="0 0 200 160">
          <polygon points="40,140 160,140 100,40" fill="none" stroke="#1565c0" stroke-width="2"/>
          <line x1="100" y1="40" x2="100" y2="140" stroke="#e91e63" stroke-dasharray="3,3"/>
          <text x="105" y="90" font-size="12">h</text>
          <text x="100" y="155" text-anchor="middle" font-size="12">a</text>
        </svg>
        <div class="formulas">
          <strong>Площа:</strong> S = ½·a·h<br>
          <strong>Периметр:</strong> P = a + b + c
        </div>
        <input type="number" id="tri-a" placeholder="Основа a" step="0.1">
        <input type="number" id="tri-h" placeholder="Висота h" step="0.1">
        <button onclick="calc('tri')">Площа</button>
        <div id="res-tri" class="result"></div>
      </div>

      <!-- Паралелограм -->
      <div class="figure">
        <h3>Паралелограм</h3>
        <svg width="220" height="140" viewBox="0 0 220 140">
          <polygon points="30,100 170,100 140,40 0,40" fill="none" stroke="#1565c0" stroke-width="2"/>
          <line x1="70" y1="40" x2="70" y2="100" stroke="#e91e63" stroke-dasharray="3,3"/>
          <text x="75" y="70" font-size="12">h</text>
          <text x="85" y="115" font-size="12">a</text>
        </svg>
        <div class="formulas">
          <strong>Площа:</strong> S = a·h<br>
          <strong>Периметр:</strong> P = 2(a + b)
        </div>
        <input type="number" id="par-a" placeholder="Сторона a" step="0.1">
        <input type="number" id="par-h" placeholder="Висота h" step="0.1">
        <button onclick="calc('par')">Площа</button>
        <div id="res-par" class="result"></div>
      </div>

      <!-- Ромб -->
      <div class="figure">
        <h3>Ромб</h3>
        <svg width="180" height="180" viewBox="0 0 180 180">
          <polygon points="90,30 160,90 90,150 20,90" fill="none" stroke="#1565c0" stroke-width="2"/>
          <line x1="20" y1="90" x2="160" y2="90" stroke="#e91e63" stroke-width="1"/>
          <line x1="90" y1="30" x2="90" y2="150" stroke="#e91e63" stroke-width="1"/>
          <text x="125" y="85" font-size="12">d₁</text>
          <text x="95" y="120" font-size="12">d₂</text>
        </svg>
        <div class="formulas">
          <strong>Площа:</strong> S = ½·d₁·d₂<br>
          <strong>Периметр:</strong> P = 4a
        </div>
        <input type="number" id="romb-d1" placeholder="d₁" step="0.1">
        <input type="number" id="romb-d2" placeholder="d₂" step="0.1">
        <button onclick="calc('romb')">Площа</button>
        <div id="res-romb" class="result"></div>
      </div>

      <!-- Трапеція -->
      <div class="figure">
        <h3>Трапеція</h3>
        <svg width="220" height="140" viewBox="0 0 220 140">
          <polygon points="50,120 170,120 140,40 80,40" fill="none" stroke="#1565c0" stroke-width="2"/>
          <line x1="110" y1="40" x2="110" y2="120" stroke="#e91e63" stroke-dasharray="3,3"/>
          <text x="115" y="80" font-size="12">h</text>
          <text x="110" y="135" text-anchor="middle" font-size="12">a</text>
          <text x="110" y="30" text-anchor="middle" font-size="12">b</text>
        </svg>
        <div class="formulas">
          <strong>Площа:</strong> S = ½·(a + b)·h<br>
          <strong>Периметр:</strong> P = a + b + c + d
        </div>
        <input type="number" id="trap-a" placeholder="Нижня осн. a" step="0.1">
        <input type="number" id="trap-b" placeholder="Верхня осн. b" step="0.1">
        <input type="number" id="trap-h" placeholder="Висота h" step="0.1">
        <button onclick="calc('trap')">Площа</button>
        <div id="res-trap" class="result"></div>
      </div>

      <!-- Коло -->
      <div class="figure">
        <h3>Коло</h3>
        <svg width="180" height="180" viewBox="0 0 180 180">
          <circle cx="90" cy="90" r="60" fill="none" stroke="#1565c0" stroke-width="2"/>
          <line x1="90" y1="90" x2="150" y2="90" stroke="#e91e63" stroke-width="1.5"/>
          <text x="120" y="85" font-size="12">r</text>
        </svg>
        <div class="formulas">
          <strong>Площа:</strong> S = πr²<br>
          <strong>Довжина:</strong> C = 2πr
        </div>
        <input type="number" id="circle-r" placeholder="Радіус r" step="0.1">
        <button onclick="calc('circle')">Обчислити</button>
        <div id="res-circle" class="result"></div>
      </div>

      <!-- Правильний багатокутник -->
      <div class="figure">
        <h3>Правильний багатокутник</h3>
        <div class="formulas">
          <strong>Площа:</strong> S = ½·P·r (P — периметр, r — радіус вписаного кола)<br>
          або S = (n·a²) / (4·tg(180°/n))
        </div>
        <input type="number" id="poly-n" placeholder="Кількість сторін n" min="3" step="1">
        <input type="number" id="poly-a" placeholder="Сторона a" step="0.1">
        <button onclick="calc('poly')">Площа</button>
        <div id="res-poly" class="result"></div>
      </div>
    </div>

    <!-- Об’ємні фігури -->
    <div id="solid" class="tab-content">
      <!-- Куб -->
      <div class="figure">
        <h3>Куб</h3>
        <svg width="200" height="200" viewBox="0 0 200 200">
          <polygon points="50,50 130,50 130,130 50,130" fill="none" stroke="#1565c0" stroke-width="2"/>
          <polygon points="80,20 160,20 160,100 80,100" fill="none" stroke="#1565c0" stroke-width="2"/>
          <line x1="50" y1="50" x2="80" y2="20" stroke="#1565c0" stroke-width="2"/>
          <line x1="130" y1="50" x2="160" y2="20" stroke="#1565c0" stroke-width="2"/>
          <line x1="130" y1="130" x2="160" y2="100" stroke="#1565c0" stroke-width="2"/>
          <line x1="50" y1="130" x2="80" y2="100" stroke="#1565c0" stroke-width="2"/>
        </svg>
        <div class="formulas">
          <strong>Об’єм:</strong> V = a³<br>
          <strong>Площа поверхні:</strong> S = 6a²
        </div>
        <input type="number" id="cube-a" placeholder="Сторона a" step="0.1">
        <button onclick="calc('cube')">Обчислити</button>
        <div id="res-cube" class="result"></div>
      </div>

      <!-- Паралелепіпед -->
      <div class="figure">
        <h3>Прямокутний паралелепіпед</h3>
        <div class="formulas">
          <strong>Об’єм:</strong> V = a·b·c<br>
          <strong>Площа поверхні:</strong> S = 2(ab + ac + bc)
        </div>
        <input type="number" id="paral-a" placeholder="a" step="0.1">
        <input type="number" id="paral-b" placeholder="b" step="0.1">
        <input type="number" id="paral-c" placeholder="c" step="0.1">
        <button onclick="calc('paral')">Обчислити</button>
        <div id="res-paral" class="result"></div>
      </div>

      <!-- Призма (трикутна) -->
      <div class="figure">
        <h3>Трикутна призма</h3>
        <div class="formulas">
          <strong>Об’єм:</strong> V = S<sub>осн</sub> · H<br>
          <strong>Площа:</strong> S = 2·S<sub>осн</sub> + P<sub>осн</sub>·H
        </div>
        <input type="number" id="prism-s" placeholder="Площа основи" step="0.1">
        <input type="number" id="prism-h" placeholder="Висота H" step="0.1">
        <button onclick="calc('prism')">Об’єм</button>
        <div id="res-prism" class="result"></div>
      </div>

      <!-- Циліндр -->
      <div class="figure">
        <h3>Циліндр</h3>
        <svg width="160" height="200" viewBox="0 0 160 200">
          <ellipse cx="80" cy="40" rx="50" ry="20" fill="none" stroke="#1565c0" stroke-width="2"/>
          <ellipse cx="80" cy="160" rx="50" ry="20" fill="none" stroke="#1565c0" stroke-width="2"/>
          <line x1="30" y1="40" x2="30" y2="160" stroke="#1565c0" stroke-width="2"/>
          <line x1="130" y1="40" x2="130" y2="160" stroke="#1565c0" stroke-width="2"/>
        </svg>
        <div class="formulas">
          <strong>Об’єм:</strong> V = πr²h<br>
          <strong>Площа поверхні:</strong> S = 2πr(r + h)
        </div>
        <input type="number" id="cyl-r" placeholder="Радіус r" step="0.1">
        <input type="number" id="cyl-h" placeholder="Висота h" step="0.1">
        <button onclick="calc('cyl')">Обчислити</button>
        <div id="res-cyl" class="result"></div>
      </div>

      <!-- Куля -->
      <div class="figure">
        <h3>Куля</h3>
        <svg width="180" height="180" viewBox="0 0 180 180">
          <circle cx="90" cy="90" r="70" fill="none" stroke="#1565c0" stroke-width="2"/>
        </svg>
        <div class="formulas">
          <strong>Об’єм:</strong> V = ⁴⁄₃πr³<br>
          <strong>Площа поверхні:</strong> S = 4πr²
        </div>
        <input type="number" id="sphere-r" placeholder="Радіус r" step="0.1">
        <button onclick="calc('sphere')">Обчислити</button>
        <div id="res-sphere" class="result"></div>
      </div>

      <!-- Конус -->
      <div class="figure">
        <h3>Конус</h3>
        <svg width="160" height="200" viewBox="0 0 160 200">
          <ellipse cx="80" cy="160" rx="50" ry="20" fill="none" stroke="#1565c0" stroke-width="2"/>
          <line x1="30" y1="160" x2="80" y2="40" stroke="#1565c0" stroke-width="2"/>
          <line x1="130" y1="160" x2="80" y2="40" stroke="#1565c0" stroke-width="2"/>
          <line x1="80" y1="40" x2="80" y2="160" stroke="#e91e63" stroke-dasharray="3,3"/>
          <text x="85" y="100" font-size="12">h</text>
        </svg>
        <div class="formulas">
          <strong>Об’єм:</strong> V = ⅓πr²h<br>
          <strong>Площа поверхні:</strong> S = πr(r + l), де l = √(r² + h²)
        </div>
        <input type="number" id="cone-r" placeholder="Радіус r" step="0.1">
        <input type="number" id="cone-h" placeholder="Висота h" step="0.1">
        <button onclick="calc('cone')">Обчислити</button>
        <div id="res-cone" class="result"></div>
      </div>

      <!-- Піраміда (чотирикутна) -->
      <div class="figure">
        <h3>Чотирикутна піраміда</h3>
        <div class="formulas">
          <strong>Об’єм:</strong> V = ⅓·S<sub>осн</sub>·H<br>
          <strong>Площа:</strong> S = S<sub>осн</sub> + S<sub>біч</sub>
        </div>
        <input type="number" id="pyr-s" placeholder="Площа основи" step="0.1">
        <input type="number" id="pyr-h" placeholder="Висота H" step="0.1">
        <button onclick="calc('pyr')">Об’єм</button>
        <div id="res-pyr" class="result"></div>
      </div>

      <!-- Тетраедр -->
      <div class="figure">
        <h3>Правильний тетраедр</h3>
        <div class="formulas">
          <strong>Об’єм:</strong> V = (a³√2)/12<br>
          <strong>Площа поверхні:</strong> S = a²√3
        </div>
        <input type="number" id="tetra-a" placeholder="Сторона a" step="0.1">
        <button onclick="calc('tetra')">Обчислити</button>
        <div id="res-tetra" class="result"></div>
      </div>
    </div>

    <footer>
      🌟 Усі формули для шкільного курсу геометрії (5–11 класи)<br>
      Автор: Твій цифровий помічник з геометрії 💡
    </footer>
  </div>

  <script>
    const PI = Math.PI;

    function openTab(name) {
      document.querySelectorAll('.tab-content').forEach(el => el.classList.remove('active'));
      document.querySelectorAll('.tab-btn').forEach(btn => btn.classList.remove('active'));
      document.getElementById(name).classList.add('active');
      event.target.classList.add('active');
    }

    function calc(type) {
      let resEl = document.getElementById(`res-${type}`);
      if (!resEl) return;

      const get = (id) => parseFloat(document.getElementById(`${type}-${id}`)?.value);

      let msg = "";
      switch(type) {
        case 'square':
          const a = get('a');
          if (a <= 0) { msg = "❌ Сторона має бути > 0"; break; }
          const S_sq = a*a;
          const P_sq = 4*a;
          const d_sq = a*Math.sqrt(2);
          msg = `S = ${S_sq.toFixed(2)} см², P = ${P_sq.toFixed(2)} см, d = ${d_sq.toFixed(2)} см`;
          break;

        case 'rect':
          const a1 = get('a'), b1 = get('b');
          if (a1 <= 0 || b1 <= 0) { msg = "❌ Сторони > 0"; break; }
          const S_r = a1*b1;
          const P_r = 2*(a1+b1);
          const d_r = Math.sqrt(a1*a1 + b1*b1);
          msg = `S = ${S_r.toFixed(2)} см², P = ${P_r.toFixed(2)} см, d = ${d_r.toFixed(2)} см`;
          break;

        case 'tri':
          const a2 = get('a'), h2 = get('h');
          if (a2 <= 0 || h2 <= 0) { msg = "❌ Дані > 0"; break; }
          msg = `S = ${(0.5*a2*h2).toFixed(2)} см²`;
          break;

        case 'par':
          const a3 = get('a'), h3 = get('h');
          if (a3 <= 0 || h3 <= 0) { msg = "❌ Дані > 0"; break; }
          msg = `S = ${(a3*h3).toFixed(2)} см²`;
          break;

        case 'romb':
          const d1 = get('d1'), d2 = get('d2');
          if (d1 <= 0 || d2 <= 0) { msg = "❌ Діагоналі > 0"; break; }
          msg = `S = ${(0.5*d1*d2).toFixed(2)} см²`;
          break;

        case 'trap':
          const a4 = get('a'), b4 = get('b'), h4 = get('h');
          if (a4 <= 0 || b4 <= 0 || h4 <= 0) { msg = "❌ Дані > 0"; break; }
          msg = `S = ${(0.5*(a4+b4)*h4).toFixed(2)} см²`;
          break;

        case 'circle':
          const r1 = get('r');
          if (r1 <= 0) { msg = "❌ Радіус > 0"; break; }
          const S_c = PI*r1*r1;
          const C_c = 2*PI*r1;
          msg = `S = ${S_c.toFixed(2)} см², C = ${C_c.toFixed(2)} см`;
          break;

        case 'poly':
          const n = parseInt(get('n')), a5 = get('a');
          if (n < 3 || a5 <= 0) { msg = "❌ n ≥ 3, a > 0"; break; }
          const S_p = (n * a5*a5) / (4 * Math.tan(PI/n));
          msg = `S = ${S_p.toFixed(2)} см²`;
          break;

        case 'cube':
          const a6 = get('a');
          if (a6 <= 0) { msg = "❌ Сторона > 0"; break; }
          const V_cu = a6**3;
          const S_cu = 6*a6*a6;
          msg = `V = ${V_cu.toFixed(2)} см³, S = ${S_cu.toFixed(2)} см²`;
          break;

        case 'paral':
          const a7 = get('a'), b7 = get('b'), c7 = get('c');
          if (a7 <= 0 || b7 <= 0 || c7 <= 0) { msg = "❌ Сторони > 0"; break; }
          const V_pa = a7*b7*c7;
          const S_pa = 2*(a7*b7 + a7*c7 + b7*c7);
          msg = `V = ${V_pa.toFixed(2)} см³, S = ${S_pa.toFixed(2)} см²`;
          break;

        case 'prism':
          const S_pr = get('s'), H_pr = get('h');
          if (S_pr <= 0 || H_pr <= 0) { msg = "❌ Дані > 0"; break; }
          msg = `V = ${(S_pr * H_pr).toFixed(2)} см³`;
          break;

        case 'cyl':
          const r2 = get('r'), h5 = get('h');
          if (r2 <= 0 || h5 <= 0) { msg = "❌ Дані > 0"; break; }
          const V_cy = PI*r2*r2*h5;
          const S_cy = 2*PI*r2*(r2 + h5);
          msg = `V = ${V_cy.toFixed(2)} см³, S = ${S_cy.toFixed(2)} см²`;
          break;

        case 'sphere':
          const r3 = get('r');
          if (r3 <= 0) { msg = "❌ Радіус > 0"; break; }
          const V_sp = (4/3)*PI*r3**3;
          const S_sp = 4*PI*r3*r3;
          msg = `V = ${V_sp.toFixed(2)} см³, S = ${S_sp.toFixed(2)} см²`;
          break;

        case 'cone':
          const r4 = get('r'), h6 = get('h');
          if (r4 <= 0 || h6 <= 0) { msg = "❌ Дані > 0"; break; }
          const V_co = (1/3)*PI*r4*r4*h6;
          const l = Math.sqrt(r4*r4 + h6*h6);
          const S_co = PI*r4*(r4 + l);
          msg = `V = ${V_co.toFixed(2)} см³, S = ${S_co.toFixed(2)} см²`;
          break;

        case 'pyr':
          const S_py = get('s'), H_py = get('h');
          if (S_py <= 0 || H_py <= 0) { msg = "❌ Дані > 0"; break; }
          msg = `V = ${(1/3 * S_py * H_py).toFixed(2)} см³`;
          break;

        case 'tetra':
          const a8 = get('a');
          if (a8 <= 0) { msg = "❌ Сторона > 0"; break; }
          const V_te = (a8**3 * Math.sqrt(2)) / 12;
          const S_te = a8*a8 * Math.sqrt(3);
          msg = `V = ${V_te.toFixed(2)} см³, S = ${S_te.toFixed(2)} см²`;
          break;

        default:
          msg = "❌ Невідома фігура";
      }

      resEl.textContent = msg;
    }
  </script>
</body>
</html>
